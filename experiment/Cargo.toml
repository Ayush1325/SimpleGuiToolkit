[package]
name = "experiment"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
sgt_core = { path="../sgt_core" }
futures = { version="0.3", features=["executor"] }
